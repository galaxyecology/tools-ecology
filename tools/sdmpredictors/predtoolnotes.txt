data_monthly = as.logical(args[4])
data_version = as.logical(args[5])
data_dataset = as.logical(args[6])
                        , monthly = data_monthly
                        , version = data_version
                        , datasets = data_dataset
         '${monthly}'
         '${version}'
         '${datasets}'

        <param name="monthly" type="boolean" label="monthly database" help="Ask for monthly data layers"/>
        <param name="version" type="boolean" label="version" help="Ask for version of data layers"/>
        <param name="datasets" type="input" label="Dataset" help="enter dataset, optional"/>
